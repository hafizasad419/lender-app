import { Router } from "express";


const collectorRouter = Router();

// collectorRouter.route("/profile/:collectorId").get(getCollectorProfile)


export default collectorRouter;                    
